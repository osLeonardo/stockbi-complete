import React from "react";
import {AreaFooter} from './styled';

export const Footer = () => {
    return (
        <AreaFooter>
            Todos os direitos reservados.
        </AreaFooter>
    );
};